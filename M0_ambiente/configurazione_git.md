# esercizio 4 

### le quattro impostazioni richieste sono : 

- git config --global user.name "Aysha Mohammed"
- git config --global user.email "aysha.mohammed@marconirovereto.it"
- git config --global init.defaultBranch main
- git config --global core.editor "code --wait" 
 
 questi sono i 4 comandi principali grazie al quale si fa la configurazione. 
 Una volta fatto ciò, come dice l consegna, è da andare a verificare se la configurazione è stata fatta correttamente e cosa da per output.


 output dato :

```text
file:z://.gitconfig	user.name=Aysha Mohammed
file:z://.gitconfig	user.email=aysha.mohammed@marconirovereto.it
file:z://.gitconfig	init.defaultbranch=main
file:z://.gitconfig	core.editor=code --wait
```